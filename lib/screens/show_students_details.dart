import 'package:flutter/material.dart';

class ShowStudentsDetails extends StatelessWidget {
  const ShowStudentsDetails({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
          title: Text(
        "Studetns Details",
      )),
    );
  }
}
